# ──────────────────────────────────────────────────────────────────────────
# RoboScene+ Gradio Demo — Python dependencies
# Install: pip install -r app/requirements.txt
# Python >= 3.10 recommended
# ──────────────────────────────────────────────────────────────────────────

# ── Gradio UI ──────────────────────────────────────────────────────────────
gradio>=4.44.0,<5.0.0          # Gradio Blocks + theme API

# ── Anthropic (Claude) ─────────────────────────────────────────────────────
anthropic>=0.34.0              # Claude API client (query_scene.py)

# ── Numerical / data ───────────────────────────────────────────────────────
numpy>=1.24.0                  # array ops, confidence maps
pandas>=2.0.0                  # dataframe displayed in Tab 2

# ── Computer vision ────────────────────────────────────────────────────────
opencv-python-headless>=4.8.0  # frame extraction, image I/O (no GUI deps)
Pillow>=10.0.0                 # image loading / thumbnail generation

# ── 3D / geometry ──────────────────────────────────────────────────────────
open3d>=0.18.0                 # point-cloud I/O, visualisation helpers
scipy>=1.11.0                  # spatial queries, convex hull, inpainting

# ── Scene graph ────────────────────────────────────────────────────────────
networkx>=3.2.0                # scene-graph construction & traversal

# ── Utilities ──────────────────────────────────────────────────────────────
tqdm>=4.66.0                   # progress bars in pipeline scripts
python-dotenv>=1.0.0           # load .env for ANTHROPIC_API_KEY
requests>=2.31.0               # HTTP utilities (model downloads etc.)

# ── Optional — only needed if running the full pipeline, not just the demo ─
# torch>=2.1.0                 # MASt3R / Gaussian Splatting backbone
# torchvision>=0.16.0
# groundingdino @ git+https://github.com/IDEA-Research/GroundingDINO.git
# segment-anything-2 @ git+https://github.com/facebookresearch/segment-anything-2.git